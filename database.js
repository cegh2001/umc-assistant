const knex = require('knex')

const database =() =>({

    const knex =require('knex')({


        Client: 'mysql',
        connection:{
            host:,
            port:,
            user:,
            password:,
            database:,
        }  
    });

    const table = '';
    const LeerInf = ()=>{
        return knex(table).select();
    };

    return{
        LeerInfo
    };
});
module.exports ={

    database
};